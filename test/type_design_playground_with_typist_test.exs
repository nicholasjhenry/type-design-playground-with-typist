defmodule TypeDesignPlaygroundWithTypistTest do
  use ExUnit.Case
  doctest TypeDesignPlaygroundWithTypist

  test "greets the world" do
    assert TypeDesignPlaygroundWithTypist.hello() == :world
  end
end
